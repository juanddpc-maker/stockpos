# Copia como .env y ajusta los valores
# Para Streamlit Cloud usa Settings → Secrets (formato TOML)

# PostgreSQL para producción (dejar vacío = SQLite local)
# DATABASE_URL=postgresql://usuario:password@host:5432/dbname

# GROQ para el agente IA (gratis en console.groq.com)
# GROQ_API_KEY=gsk_...
